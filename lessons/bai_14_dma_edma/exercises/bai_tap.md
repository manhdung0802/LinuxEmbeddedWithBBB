# Bài tập - Bài 14 DMA EDMA

1. So sánh thời gian của DMA memcpy vs CPU memcpy cho buffer 1MB — viết benchmark
2. Tìm hiểu `scatter-gather DMA`: `dmaengine_prep_slave_sg()` dùng trong trường hợp nào?
3. Giải thích flow từ SPI peripheral event → EDMA channel → PaRAM → memory khi dùng SPI DMA
